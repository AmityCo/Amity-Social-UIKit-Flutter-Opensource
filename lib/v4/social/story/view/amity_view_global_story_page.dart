import 'package:flutter/material.dart';

class AmityViewGlobalStoryPage extends StatelessWidget {
  const AmityViewGlobalStoryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}